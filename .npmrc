# Some SDK 56 peer-dependency ranges (expo-router / reanimated / worklets)
# conflict under npm's strict resolver. legacy-peer-deps lets npm install
# the Expo-recommended set without erroring, both locally and on EAS Build.
legacy-peer-deps=true
